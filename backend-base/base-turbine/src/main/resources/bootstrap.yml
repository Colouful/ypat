spring:
  application:
    name: turbine

  cloud:
    config:
      uri: http://localhost:8888

  rabbitmq:
    addresses: ${YPAT_RABBITMQ_URL:amqp://localhost:5672}
    username: ${YPAT_RABBITMQ_USERNAME:guest}
    password: ${YPAT_RABBITMQ_PASSWORD:}


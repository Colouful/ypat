spring:
  application:
    name: config

  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/xiaoding/base-config-repo.git
          username:
          password:

  rabbitmq:
        addresses: ${vcap.services.${PREFIX:}rabbitmq.credentials.uri:amqp://${RABBITMQ_HOST:localhost}:${RABBITMQ_PORT:5672}}
        username: ${YPAT_RABBITMQ_USERNAME:guest}
        password: ${YPAT_RABBITMQ_PASSWORD:}

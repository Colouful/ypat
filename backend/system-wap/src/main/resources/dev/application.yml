server:
  port: 8081
  tomcat:
    max-http-post-size: -1
spring:
  http:
    multipart:
      max-file-size: 200Mb
      max-request-size: 400Mb
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  redis:
    host: ${YPAT_REDIS_HOST:localhost}
    port: ${YPAT_REDIS_PORT:6379}
    password: ${YPAT_REDIS_PASSWORD:}
    pool:
      max-idle: 8
      min-idle: 0
      max-active: 8
      max-wait: -1
  jackson:
    time-zone: GMT+8
  jpa:
    # 手动指定，避免 Druid 触发 Class.forName("com.mysql.jdbc.Driver") 导致
    # Spring Boot 1.5.9 LaunchedURLClassLoader.definePackageIfNecessary NPE
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        format_sql: false
    show-sql: false
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    preferIpAddress: true

system:
  third:
    fdfs_path: ${YPAT_FDFS_PUBLIC_BASE_URL:http://localhost:8888}


##FeignClient超时设置
---
spring.cloud.loadbalancer.retry.enabled: false
#hystrix.command.default.execution.isolation.thread.timeoutInMilliseconds: 10000
ribbon.ConnectTimeout: 250
ribbon.ReadTimeout: 5000
ribbon.OkToRetryOnAllOperations: false
ribbon.MaxAutoRetriesNextServer: 0
ribbon.MaxAutoRetries: 0

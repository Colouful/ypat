spring:
  application:
    name: zipkin
#  profiles:
#    active: development
  cloud:
    config:
      uri: http://localhost:8888

  rabbitmq:
        addresses: amqp://localhost:5672
        username: ddd
        password: ddd

encrypt:
  failOnError: false


system-restapi:
  build: ./system-restapi
  volumes:
   - /logs:/logs
  environment:
   - TZ=Asia/Shanghai
  ports:
   - "9081:9081"

system-wap:
  build: ./system-wap
  volumes:
   - /logs:/logs
  environment:
   - TZ=Asia/Shanghai
  ports:
   - "8081:8081"

system-web:
  build: ./system-web
  volumes:
   - /logs:/logs
  environment:
   - TZ=Asia/Shanghai
  ports:
   - "8082:8082"
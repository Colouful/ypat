export interface AreaOption { label: string; value: string }
export interface AreaInfo { provinceData: AreaOption[]; cityData: AreaOption[][]; areaData: AreaOption[][][] }
export interface ParamInfo { realname: string }

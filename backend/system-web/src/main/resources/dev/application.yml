server:
  port: 8082
spring:
  http:
    multipart:
      max-request-size: 2MB
      max-file-size: 2MB
  thymeleaf:
    cache: false
  devtools:
    restart:
      enabled: true
  redis:
    host: ${YPAT_REDIS_HOST:localhost}
    port: ${YPAT_REDIS_PORT:6379}
    password: ${YPAT_REDIS_PASSWORD:}
    pool:
      max-idle: 8
      min-idle: 0
      max-active: 8
      max-wait: -1
  jackson:
    time-zone: GMT+8
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    preferIpAddress: true

system:
  third:
    # 本地开发上传到远程 FastDFS，返回可直接预览的 HTTPS 公网地址。
    fdfs_path: ${YPAT_FDFS_PUBLIC_BASE_URL:https://fastdfs.panghu.work/}



##FeignClient超时设置
---
spring.cloud.loadbalancer.retry.enabled: false
#hystrix.command.default.execution.isolation.thread.timeoutInMilliseconds: 10000
ribbon.ConnectTimeout: 250
ribbon.ReadTimeout: 5000
ribbon.OkToRetryOnAllOperations: false
ribbon.MaxAutoRetriesNextServer: 0
ribbon.MaxAutoRetries: 0

#spring devtools 更改类程序文件不使用热部署自动重启
#spring.devtools.restart.enabled: false
#spring.devtools.livereload.enabled: false
#spring.thymeleaf.cache: false
